mixin reviewCardWithTour(review, tour)
    .reviews__card
        .reviews__avatar
            img.reviews__avatar-img(src=`/img/users/${review.user.photo}`, alt=`${review.user.name}`)
            h6.reviews__user= review.user.name
        .reviews__avatar
            h2.reviews__tour= tour.name
        p.reviews__text= review.review
        .reviews__rating
            each star in [1, 2, 3, 4, 5]
                svg.reviews__star(class= `reviews__star--${review.rating >= star ? 'active' : 'inactive'}`)
                    use(xlink:href='/img/icons.svg#icon-star')
        form.form.form--delete
            input#reviewId(type='hidden' name='reviewIdwithName' value=`${review.id}`)
            .form__group
                button.btn.btn--red Delete

mixin reviewCardWithoutTour(review)
    .reviews__card
        .reviews__avatar
            img.reviews__avatar-img(src=`/img/users/${review.user.photo}`, alt=`${review.user.name}`)
            h6.reviews__user= review.user.name
        p.reviews__text= review.review
        .reviews__rating
            each star in [1, 2, 3, 4, 5]
                svg.reviews__star(class= `reviews__star--${review.rating >= star ? 'active' : 'inactive'}`)
                    use(xlink:href='/img/icons.svg#icon-star')

mixin manageReview(review)
    .reviews__card
        .reviews__avatar
            img.reviews__avatar-img(src=`/img/users/${review.user.photo}`, alt=`${review.user.name}`)
            h6.reviews__user= review.user.name
        p.reviews__text= review.review
        .reviews__rating
            each star in [1, 2, 3, 4, 5]
                svg.reviews__star(class= `reviews__star--${review.rating >= star ? 'active' : 'inactive'}`)
                    use(xlink:href='/img/icons.svg#icon-star')
        form.form.form--delete
            input#reviewId(type='hidden' name='reviewIdwithName' value=`${review.id}`)
            .form__group
                button.btn.btn--red Delete

mixin manageUser(user)
    .card
        .card__details
            .card__data
                img.reviews__avatar-img(src=`/img/users/${user.photo}`, alt=`${user.name}`)
                .card__footer
                    p
                        span.card__footer-text Name:-
                        | 
                        span.card__footer-value= user.name
                    p.card__ratings
                        span.card__footer-text Email:-
                        | 
                        span.card__footer-value= user.email
